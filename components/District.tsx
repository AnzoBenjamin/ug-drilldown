// DistrictComponent.js
const District = ({ districtName, counties }) => {
    return (
      <div>
        <h2>{districtName}</h2>
        {/* Display information about the district and its counties */}
      </div>
    );
  };
  
  export default District;
  